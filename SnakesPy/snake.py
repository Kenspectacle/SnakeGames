class Snake:
    def __init__(self, state, head, segments, current_direction):
        self.state = state
        self.head = head
        self.segments = segments
        self.current_direction  = current_direction
    
    